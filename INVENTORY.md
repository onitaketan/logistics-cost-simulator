# 2店舗 在庫管理システム — 操作マニュアル / 引き継ぎ資料

バーコード（JANコード）で在庫を管理する、2店舗リアルタイム共有のWebアプリ（PWA）です。

## URL

| 用途 | URL |
|---|---|
| 店長用（PC/スマホ） | https://onitaketan.github.io/logistics-cost-simulator/inventory-cloud.html |
| スタッフ用（スマホ） | https://onitaketan.github.io/logistics-cost-simulator/inventory-staff.html |

※スマホでは各URLを開き「ホーム画面に追加」でアプリのように起動できます（PWA・費用不要）。

## 役割と操作

### 店長（`inventory-cloud.html`）※PINでログイン
- **入荷**：バーコード読取＋商品名・数量・棚番号・**大分類／小分類（枝分け）**・**店内識別コード**で在庫を登録（初回登録後は次回から自動表示。分類・コードは任意）
- **店舗間移動**：移動元→移動先で在庫数を自動更新
- **締め（確定）**：その日の売上をレジと突き合わせ、必要なら数を修正して確定
- **設定**：店舗名の変更、PINの変更、この端末のスタッフ名
- 初回は「店長ログイン」から**PIN（4桁以上）を設定**して利用開始

### 分類（枝分け）と店内識別コードについて
- **分類**：`大分類 → 小分類`の2階層で整理（例：ゲーム→ファミコン／本→漫画）。入荷時に打つと候補が学習され、次回から選べます。
- **店内識別コード**：JANとは別に付ける店独自の管理番号（任意）。JANの無い商品や手書きシール番号に使えます。**売れた／在庫検索でこのコードでも探せます。**

### スタッフ（`inventory-staff.html`）※ログイン不要
- **売れた**：バーコード読取／タップ／売れ筋／**分類で絞り込み**／**店内コード入力**で選んで在庫を −1（在庫0以下でも記録し、店長の締めでズレを吸収）
- **直近の売上**：本日・締め前の売上をその場で取り消せる（押し間違いのリカバリ）
- **在庫**：全店舗の在庫・棚番号を検索（タイトル・機種・JAN・**店内コード**で検索可）
- **売上**：日別に「何が・何個」売れたかを表で確認（金額なし）

## データについて
- 保存先：**Supabase（クラウドDB・東京リージョン）**。全端末で共有・自動バックアップ。機種変更やキャッシュ削除でも消えません。
- 通信は HTTPS で暗号化。店長操作は**サーバー側で検証するPIN**で保護（連続失敗でロック）。
- バックアップ：店長画面や、必要に応じてCSV出力で取得できます。

## 設計上の要点（安全対策）
- 在庫更新はすべてサーバー側の関数（RPC）を経由し**原子的に処理** → 同時に売れても数がズレない
- 「今日」は**日本時間(JST)**でサーバーが判定
- 売上取消は**冪等**（二重クリックしても在庫が二重に戻らない）
- 在庫はライブ表示、店長の**日次締め**で監査・確定

## 費用の目安
- Web公開（GitHub Pages）：無料
- クラウドDB（Supabase）：無料枠内で運用可（規模拡大時のみ有料プラン検討）
- アプリストア公開を行う場合のみ：Apple $99/年、Google $25（初回）

## 本採用時の引き継ぎ（推奨）
1. **お店専用のSupabaseプロジェクトに分離**（データ完全分離・所有明確化）
2. GitHub / Supabase アカウントの**所有権をお店側へ移管**
3. 定期バックアップ運用の確認

## 今後の拡張候補
- JANコードから商品名を自動取得（本＝openBD無料／ゲーム等＝楽天API）
- 通信断でも売上を貯めて後で同期（完全オフライン対応）
- 価格・状態ランク（A/B/C）の管理項目
- リアルタイム自動反映（現在は15〜20秒ごと＋更新）
